import React from 'react';

const Compat = (props) => {
    const { tarot_candidate, tarot_emp, emp, candidate } = props;

  return (
      <div className="innoviant-wrapper">
          <h1 className="compat-title">Анализ совместимости</h1>

          <h2>Карты таро </h2>
      </div>
  );
};

export default Compat;
