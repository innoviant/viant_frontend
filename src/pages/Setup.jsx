import React from 'react';
import {URLs} from "../__data__/URLs";

const Setup = () => {
  return (
      <div className="innoviant-wrapper">
          <h1>Добавление работников и кандидата для анализа</h1>
          <a href={URLs.compat}>Расчитать</a>
      </div>
  );
};

export default Setup;
